import * as AllPhosphorIcons from "@phosphor-icons/react/ssr";

// Then you can check for valid names:
 console.log(AllPhosphorIcons.HouseLineIcon); // This will show the component if it exists
 console.log(Object.keys(AllPhosphorIcons)); // This will list all available icon names